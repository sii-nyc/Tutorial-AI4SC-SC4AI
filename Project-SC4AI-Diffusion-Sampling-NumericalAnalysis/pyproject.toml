[project]
name = "diffusion-na"
version = "0.1.0"
description = "反向扩散采样的数值分析 (SC4AI open project)"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "matplotlib>=3.8",
    "torch>=2.2",
    "scikit-learn>=1.4",
    "jupyter>=1.0",
    "ipykernel>=6.29",
    "pytest>=8.0",
]

[tool.uv]
package = false
