# 《反向扩散采样的数值分析》最终验收报告

- 审计日期：2026-05-30
- 交付物：「反向扩散采样的数值分析.ipynb」（自包含）+ `diffusion_na.py` + `build_notebook.py` / `inline_library.py` + `tests/test_diffusion_na.py` + `README.md` + `figures/fig01..fig12`
- 课程：同济《人工智能数理基础（高级）》SC4AI 开放课题（占总分 50%，评分 = 选题价值 20% + 方法创新 20% + 报告完整 40% + 代码 20%）
- 验收方式：独立审计——逐条 Read 源码、用隔离解释器 `.venv` 从零重算关键数字、视觉检查 12 张 PNG、扫描全文措辞。**未默认核心设定为正确，所有命题独立复核。**

---

## 0. 一句话总评

**可按现状交付，且属高质量档（预估 A / 优）。** 数学核心 22 条独立命题逐位经得起复核、代码与其声称严格一致且 23/23 测试稳定通过、报告结构完整并具学术诚实度；**未发现任何 blocker**。现存问题全为可在数十分钟内修完的措辞/符号/元信息瑕疵（should-fix 6 条、nice-to-have 多条），修掉后报告完整与方法创新两轴几乎满分。

---

## 1. 复现性核验小结

| 核验项 | 结果 |
|---|---|
| `pytest -q`（`.venv` 三次运行） | **23 passed, 0 failed**，无 skip / error / warning，每次约 4 s，结果一致。（注：任务简报写「24」，实际为 23；README 第 17 行已正确写「23 项」。） |
| 关键数字重算（从零 NumPy / `solve_ivp`，与库逐位对照） | 全部吻合，见下 |
| 确定性 | 解析量纯闭式（固定常数，无 RNG）；ODE 用 `solve_ivp(RK45, rtol=atol=1e-10)` 复现到 ~1e-12；随机采样器走显式种子 `np.random.default_rng`，同种子两次调用 `np.array_equal=True`（bit-for-bit）。 |
| 网络实验复现 | `build_notebook.py:91` 硬编码 `DEVICE='cpu'`、`SEED=2026`、全程显式种子 → 设备无关、bit-for-bit 可复现（MPS 为 opt-in）。 |
| notebook 自包含性 | `inline_library.py` 顶部内联整库源码、`exec` 后注册 `sys.modules['diffusion_na']`，后续 `import` 解析到内联模块；无需外部 `.py` 即可运行。 |

**关键数字（库值 == 我的独立重算）：**

- `v(eps=1e-3) = 0.2500824579667902`（数据 N(0,0.5²)，故 `v(eps) ≈ s0² = 0.25`，非 0.75）；`v(0)=0.25=s0²`、`v(1)=0.99996≈1`。
- `reverse_variance_exact(eps)=0.25008245796` 与 `v(eps)` 之差 = **4.64e-12**（确认反向方差 ODE 末端 = `v(eps)`，因子 2 正确）。
- `a(1)=10.000647807217934`；`2/a(1)=critical_dt(1)=0.19998704469`（EM 显式稳定界 h≤2/|a| 在 t=1 处约 0.2）。
- `c_theory=0.3579839042245619`、`c_emp=0.3579812290143878`，**相对误差 7.47e-06**（强力支撑报告「<0.1%」）。
- **「漏因子 2」反例可复现**：将反向方差 ODE 的 `-2aV+β` 改为 `-aV+β`，末端 = `0.7588850620537133`（≈0.76），精确复现报告记录的错误值——证明因子 2 是 load-bearing。

> 环境透明披露：本会话与前序会话的工具输出通道曾间歇性退化为完全空（与地面真值 `anomalies` 一致）。所有科学数字均来自干净运行、并经两名审计员通过 stdout 与 `/tmp` 文件 Read 双路核对、bit-for-bit 一致，结论可靠；此为 harness 事件，**非交付物缺陷**。

**复现性结论：稳健可信。**

---

## 2. 四轴评估与总体预估档位

| 评分轴（权重） | 档位 | 简评 |
|---|---|---|
| **选题价值 20%** | **强** | 把「反向扩散采样系统」当成数值积分对象，用后向误差分析 / 修正方程 / 绝对稳定域 / 加性 vs 乘性强阶等数值分析工具切入，立意清晰、紧扣 SC4AI 与 Lect5/Lect6；将「采样修正方程」与「SGD 修正方程」统一为同一工具，呼应课程主线得体。 |
| **方法创新 20%** | **强** | 在可解析的高斯设定下给出一整套可证伪结论：修正方程闭式系数 c 与 Richardson 经验值 7.5e-6 级吻合、PF-ODE 闭式解 x∝√v、加性噪声强阶=1（澄清「1/2 仅对乘性噪声」的常见误解）、误差三分解、log-FPE 恒等式残差核验。创新点扎实且自检充分。 |
| **报告完整 40%** | **强** | 结构严格连号（Part 0–VI、§1–§13 无缺号、实验①–⑩齐全）；摘要 (i)–(vi)、§0.4 贡献 1–6、§13 结论 1–6 三处一一对应、措辞一致；每实验配独立「分析」段与定量 assert；附录 A 复现 / B 超参 / C 自检齐全；引用 Song/Anderson/Vincent/DPM-Solver 得体；中文术语统一、排版规范。**扣分仅来自 3 处过程痕迹与若干符号/措辞张力**（见问题清单），均极易修复。预估高分段（约 35–38/40）。 |
| **代码 20%** | **强** | 实现与声称数学严格一致、无静默 bug / 广播错误、数值稳定性处理到位（logsumexp、a→0 护栏、`unicode_minus`）；测试为可证伪硬断言（有限差分 / 蒙特卡洛互校 / 因子 2 锁死），非自证式；复现性稳健。预估约 19–20/20。 |

**总体预估档位：A / 优（高分段）。** 四轴均为「强」，无任何阻断性缺陷；现状即可交付。

---

## 3. 亮点清单

1. **因子 2 正确且经反例验证**：反向方差 ODE `dV/dτ=-2aV+β`（`diffusion_na.py:135`）末端 = `v(eps)`（差 4.6e-12）；去掉因子 2 末端变 0.7589——bug 与正确值差异巨大，归因准确。
2. **修正方程理论 ↔ 经验 7.5e-6 级吻合**：`c_theory=0.357984` vs `c_emp=0.357981`，远优于报告所称 <0.1%；且解析二阶导 V'' 与 `solve_ivp` 解的有限差分对照 maxdiff=2.69e-07、a'(t) 与差分 maxdiff=7.2e-09，证明是闭式推导而非差分近似。
3. **log-FPE 恒等式** ∂_t log p = ½β[d+x·s+‖s‖²+∇·s] 经手工代数 + 有限差分双重确认（解析 score 残差 6.66e-16，机器零；扰动 δ=0.3 残差单调增）。
4. **PF-ODE 闭式解 x∝√v** 推导逐位正确，`pf_ode_exact(2)=1.00018` 与闭式吻合，Heun 收敛到机器精度。
5. **加性噪声强阶=1**（实测 1.018）vs 乘性 GBM 1/2（实测 0.552），有效澄清教科书常见误解。
6. **误差三分解自洽**：e_prior=1.62e-5、e_disc=2.46e-2、e_score=4.45e-3，三源和 2.906e-2 ≈ 实测总 2.904e-2，且明确标注「量级归因、非严格上界」。
7. **12 张图全部中文字体正常**（PingFang/Heiti，无豆腐块、负号正常），标注斜率/系数与独立重算逐一吻合（fig03 1.035、fig05 1.02/0.55、fig08a 0.85/1.95、fig12 1.09 等），且为真实执行产物（12 次 savefig、内嵌 12 张 PNG、execution_count 连续）。
8. **学术诚实度高**：多处主动写「不应声称机器精度」「诚实负结果」「朴素指数法不一定胜过 Euler」「不作硬断言」，并主动暴露并改正早期「强阶≈1/2」误述。

---

## 4. 问题清单（去重合并，分级）

### 4.1 Blocker（阻断交付）

**无。** 所有数学命题、代码、测试、图均经独立复核成立。

### 4.2 Should-fix（建议交付前修复）

**S1. 反向时间漂移符号文字自相矛盾**
- 位置：`build_notebook.py:148`（§2 时间约定表「反向时间」行，写 `dY=-a(1-τ)Y dτ`）、`build_notebook.py:189`（§3，写 `dY=a(t)Y dt`，正号）、`build_notebook.py:202`（§4 用 `F=-2aV+β`）。
- 问题：三处符号约定在文字层面冲突。若按 `:189` 字面取正号漂移，方差 ODE 应为 `+2aV+β`，将得到发散错误真值；纯数学读者照此推导会出错。
- 证据：代入 `+2aV+β` 用 `solve_ivp` 末端 = 740131（发散）；`-2aV+β` 末端 = 0.25008 = `v(eps)`。代码 `:135`/`:153` 用的均是正确的「-a 漂移 / -2a 方差」分支，**代码无误，纯文字符号不一致**。
- 修法：统一为 τ=1-t 下 `dY=-a(1-τ)Y dτ+√β dW̄`，把 §3 `:189` 的 `dY=a(t)Y dt` 改为一致的 -a 形式；其下递推 `x_{n+1}=(1-ah)x_n` 本就对应 -a 漂移，改后全文自洽。

**S2. 缺少 t↔τ 变量替换的显式说明（S1 的根源）**
- 位置：`build_notebook.py:116`（§1 Anderson 反向 SDE，前向时间 t，dt<0）vs `:134`（§2 漂移 a(t)>0）。
- 问题：报告未把「dX=a x dt（t 递减）」与「dY=-a Y dτ（τ 递增）」的等价性显式写清，留下符号陷阱。代码全对（`em_variance_recursion(400)=0.25011`、`reverse_sample('em',400)=0.24990`、`v(eps)=0.25008` 一致）。
- 修法：§3 加一行——在 τ=1-t、dτ=-dt 下，`dX=a(t)X dt` 等价于 `dY=-a(1-τ)Y dτ`，故方差 ODE `dV/dτ=-2aV+β`，消除表面冲突。

**S3.「数值完全吻合」+「相对误差=0.00%」过度措辞**
- 位置：摘要(cell0)、§4 分析(cell11)、§13 结论(cell34)、附录 C(cell35) 共 4 处「数值完全吻合」；配 cell10 打印「相对误差=0.00%」。
- 问题：理论手推积分与 Richardson 最小二乘外推不可能 bit-for-bit 相等；真实相对误差约 7.5e-6（0.00075%）。「完全吻合」+「0.00%」对答辩老师属可一眼质疑的夸张（虽方向诚实、量级极小，打印精度 `%.2f%%` 掩盖了非零差）。
- 证据：c_theory=0.357984、c_emp=0.357981，rel=7.5e-6；cell10 输出「相对误差=0.00%」。
- 修法：改为「数值高度吻合（相对误差 ~7.5e-6，<0.1%）」；cell10 的 print 改用 `%.2e` 或 `%.4f%%` 显示真实非零差。

**S4. §12 误差地板叙述与图、与实测符号存在张力**
- 位置：§12 正文(cell32 markdown) + `figures/fig12_transfer.png` + cell32 输出。
- 问题：(a) 正文用正向 ρ=5%/10% 画「误差地板」，但实测网络等效相对偏差 ρ̂=-0.0362（负号），方向与所画地板相反；(b) 图中黑色网络曲线并未真正触底成水平地板，最细步仍降到 8.11e-5（比 ρ=5% 地板低一个量级以上）。即「离散↔逼近误差交叉点」这一核心卖点在本次实测网络上**并未真正显现**（网络太准、地板低于最细步），却被叙述为已观测现象。
- 证据：cell32 输出「网络 ρ̂=-0.0362 R²=0.996；网络实测最细步 err=8.11e-05」；fig12 黑×单调降至 ~8e-5 无平台，橙/绿在细步区变平于 ~2e-2/3e-2。
- 修法：§12 明确写出 ρ̂ 为负号（网络系统性略低估 score）及其含义；诚实说明「本次网络精度足够高（地板低于所测最细步），故未在本步数范围内观测到交叉/地板，交叉机制由合成正 ρ 曲线示意」，避免把示意当成实测显现。

**S5. 正文残留开发/AI 协作过程痕迹（3 处，合并）**
- 位置：§5 分析(`build_notebook.py:321`)「设计初稿」；§9 实验⑦分析(`:481`)「（审查意见采纳点）」；§11 实验⑨分析(`:580`)「（审查采纳点）」。
- 问题：正式交付报告不应暴露草稿/迭代/评审流程的内部元注释。
- 证据：脚本全文扫描确认上述三处为全部命中（「迭代」「AI 方法」等命中均为正当技术/课程用语）。
- 修法：删去「设计初稿」整句或改为中性结论表述；删除两处「（审查…采纳点）」括注，其余学术表述保留。建议全局再搜一次「初稿/审查/采纳点/草稿/TODO/待办」确认清零。

**S6.（审计环境，非交付物）输出通道间歇退化**
- 位置：审计会话 harness 输出通道。
- 问题：通道曾退化为完全空（本会话亦复现）。所有结论均有干净运行的一手证据、并经双路核对 bit-for-bit 一致。
- 修法：无需改交付物；仅记录环境事件。复核可重跑 `.venv` 脚本与 `pytest -q`。

### 4.3 Nice-to-have（可选润色）

- **N1.** §6/实验④「v→1 使 a 增大」因果叙述略不准确：`a=β(1/v-½)`，t→1 处是 β→20 主导 a 峰值，v→1 反而使 1/v-½→½（该因子取最小）。建议(`:335`)改为「β(t)→20 主导 a 峰值，此时 1/v→1 该因子反趋最小值 ½」。
- **N2.** §8 PF-ODE 符号(`:404`)：`pf_ode_drift` 返回前向漂移 `+½β(1/v-1)x`，反向采样 `x←x-h·f_pf`（`:244`）取负号未在文字点明。建议注明前向漂移与反向步的符号关系。
- **N3.** §4 LaTeX「a(1-s)^2」括号歧义（上标 2 紧贴右括号易读成对 (1-s) 平方）。建议改写为「[a(1-s)]^2」。代码 `:199` 为 `a**2*V` 无误，仅排版。
- **N4.** 测试 `test_modified_eq_coefficient_consistency`(`:101-105`) 阈值 <0.1（10%），而实际 rel=7.5e-6，裕度 1.3 万倍；报告称 <0.1%。建议收紧至 1e-3（仍留 ~130x 裕度），使测试强度与主张相称。
- **N5.** `build_notebook.py:91` `DEVICE='cpu'` 硬编码保证 bit-for-bit（优点）；切 MPS 会使网络断言数值变化。建议 README 补一句：默认 CPU 保 bit-for-bit，切 MPS 为可选且改变网络实验数值。
- **N6.** README(`24–25`) 与 `build_notebook.py` docstring 把开发期构建流水线（build→nbconvert→inline→再 nbconvert）写进交付件。建议精简或移入不随交付提交的 DEV_NOTES，并明确区分两个产物（import 版 / 自包含交付版），提示评阅无需重跑。
- **N7.** 摘要「机器精度参照」「降低误差常数」与正文「不应声称机器精度」「朴素指数法不一定胜过 Euler」有轻微乐观张力（结论自洽）。建议摘要把「机器精度参照」限定为「高斯设定下有闭式精确解 x∝√v，可作高精度参照」，并补「朴素指数化增益有限（详见实验⑥）」。
- **N8.** 库 docstring/摘要「每个解析公式均有数值核验」为全称断言，但 23 项测试未逐一覆盖 `energy_distance`/`bures_w2`/`gmm` 系列等导出函数。建议弱化为「核心解析公式均有数值核验」，或补几条 GMM score / energy_distance 单测。
- **N9.** fig08a 图标注 Euler 斜率 0.85 与正文「≈1」轻微张力（受最粗 NFE=4 非渐近点拉低）；fig04 两条 c 线像素级重合图例显冗余；fig02 quiver 24×24 偏密。均为可读性微瑕，建议图注说明或降采样，不影响结论。
- **N10.** 实验① 正文「以斜率≈1 的直线下降」措辞略强（实拟合 1.0345，粗端对数图略弯）。可注「斜率 1.03（粗步含高阶项致略偏 1）」。

---

## 5. 结论与交付前必做项

**结论：交付物质量优秀，可按现状提交，预估 A / 优档（高分段）。** 数学正确、代码可靠、报告完整、复现性稳健，无任何阻断性缺陷。

**交付前必做项（仅 should-fix，约 30–45 分钟可完成）：**

1. 统一反向时间漂移符号（S1）并补 t↔τ 变量替换说明（S2）——消除唯一会误导纯数学读者的符号陷阱。
2. 把「数值完全吻合 / 0.00%」改为带真实相对误差的精确措辞（S3）。
3. 修正 §12 误差地板叙述，写明 ρ̂ 负号并诚实说明交叉点未在本步数范围内实测显现（S4）。
4. 删除 3 处开发/AI 协作过程痕迹（S5）。

nice-to-have 可视时间选做，不影响交付。
